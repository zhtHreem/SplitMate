{
  "buildCommand": "npm install --force && cd client && npm install --force && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --force && cd client && npm install --force"
}